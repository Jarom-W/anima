apiVersion: v1
kind: Service
metadata:
  name: second-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: second-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
